// Panel de información de inspección (placeholder)
export function showPanel(info) {
  // Aquí se puede crear un panel flotante o modal con la información de la inspección
  alert('Inspección: ' + (info?.name || 'Sin nombre'));
}
